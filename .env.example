# --- Server ---
PORT=3000

# --- Database ---
# Local dev: mongodb://127.0.0.1:27017/patient_registration
# Production (Render): a MongoDB Atlas free-tier (M0) connection string, e.g.
# mongodb+srv://<user>:<password>@<cluster>.mongodb.net/patient_registration?retryWrites=true&w=majority
MONGODB_URI=mongodb://127.0.0.1:27017/patient_registration

# --- Vapi webhook security ---
# Shared secret Vapi sends back in the "x-vapi-secret" header on every
# webhook call. Set the same value in the Vapi assistant's serverUrlSecret.
VAPI_SERVER_SECRET=change-me

# --- Used only by scripts/setup-vapi.sh (not read by the NestJS app) ---
# Your Vapi private API key, from the Vapi dashboard (Settings > API Keys).
VAPI_API_KEY=
# Your Google AI Studio / Gemini API key. The script creates a "custom-llm"
# Vapi credential from this and points it at Google's OpenAI-compatible
# Gemini endpoint — NOT Vapi's native "Google" provider integration, whose
# dashboard credential validator currently rejects newly-created Gemini keys
# (it hardcodes a test call against a deprecated model). See README section 9.
GEMINI_API_KEY=
# Public HTTPS URL of this API once deployed (e.g. your Render domain), used
# as the tool-call serverUrl when creating the Vapi assistant.
PUBLIC_API_BASE_URL=https://your-app.onrender.com
